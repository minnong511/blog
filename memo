#__init__ 은 파이썬의 기본 패키지에 해당한다 .
# __init__.py 를 이용하면 패키지로 생성할 수 있음
ex)
 sound/

        __init__.py
        formats/
                __init__.py
                wavread.py


 ex) 파일을 불러오는 방법
 sol1)

 import sound.formats.wavread

 sol2)

 from sound.formats import echo

새로운 궁금증 git은 어떻게 사용하냐?
gitignore 이거 파일 어캐 만드냐...?

# git 파일 그냥 지우면 초기화된다.

# 파이참에서 파일 제작 -> 깃허브연동 -> 서버연동 -> 서버에서 가상환경 작동시키기

# 슈벌탱 파일 배포 왜 이렇게 어려운거야 슈발!!!!!
# 다 죽여벌랑!!!!!!!!!! 슈바!!!!!!!!!


제발 오류코드를 잘 읽어볼 것 !!

오!류!코!드! 야발 !!!!!!!!!!

자 앞으로 오류코드를 잘 읽어볼 것 , 멘탈 터지면 산책 할 것 , 오류가 생기면 다시 할 것

폴더 디렉토리 blog/templates/blog 로 하는거는 찾기 쉽게 하기 위해서이다

깃허브 에서 pull 하는 폴더는 함부러 건드리지 말자

쿼리셋이란?
-- 전달받은 모델의 객체 목록 .
즉 데이터베이스로부터 데이터를 읽고 필터를 걸거나 정렬할 수 있다

